# 1. Setup the directory structure
chmod +x setup-wwwapp.sh
./setup-wwwapp.sh

# 2. Make sure you have the Docker files in current directory
ls -la Dockerfile nginx.conf default.conf supervisord.conf

# 3. Build and start
chmod +x wwwapp-podman.sh
./wwwapp-podman.sh build
./wwwapp-podman.sh start

./wwwapp-podman.sh start-debug

-- remove container
./wwwapp-podman.sh clean
podman rmi wwwapp


in pod restart nginx 

Troubleshoot runnign instance 
netstat -tlnp | grep :8001

# Check if port 8001 is actually bound on the host
netstat -tlnp | grep :8001
ss -tlnp | grep :8001

# Check what podman thinks about port mapping
podman port wwwapp

# Check container network details
podman inspect wwwapp | grep -A 10 -B 10 "PortBindings\|NetworkMode"


# Verify container is actually running
./wwwapp-podman.sh status

# Check if nginx is listening inside the container
./wwwapp-podman.sh shell
netstat -tlnp | grep :80
# or
ss -tlnp | grep :80
curl http://localhost:80/health
exit


# Stop nginx first
nginx -s stop
# or
pkill nginx

# Then restart
nginx

# Check if it's listening
netstat -tlnp | grep :80

